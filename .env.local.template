# ---- Supabase (client-side, safe to expose) ----
VITE_SUPABASE_URL="[REPLACE_ME]"                 # e.g. https://xxxx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY="[REPLACE_ME]"     # anon / publishable key
VITE_SUPABASE_PROJECT_ID="[REPLACE_ME]"

# ---- Supabase (server-side, used during SSR) ----
SUPABASE_URL="[REPLACE_ME]"
SUPABASE_PUBLISHABLE_KEY="[REPLACE_ME]"
SUPABASE_SERVICE_ROLE_KEY="[REPLACE_ME]"         # server only, never expose

# ---- AI provider (Anthropic Claude API; server only) ----
ANTHROPIC_API_KEY="[REPLACE_ME]"
ANTHROPIC_MODEL="claude-sonnet-5"

# ---- Payments (Whop) ----
VITE_WHOP_CHECKOUT_URL="https://whop.com/mind-management-academy-hq/your-ai-business-compass"
WHOP_API_KEY="[REPLACE_ME]"                      # Whop Dashboard -> Developer -> API Keys
WHOP_ACCOUNT_ID="[REPLACE_ME]"                   # biz_xxxxxxxx
WHOP_PLAN_ID="[REPLACE_ME]"                      # plan_xxxxxxxx, your $9.99 one-time plan
WHOP_WEBHOOK_SECRET="[REPLACE_ME]"               # ws_xxxxxxxx, from the webhook you create
